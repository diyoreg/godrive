# 🔧 Автоматическая инициализация базы данных PostgreSQL

## ✨ Автоматическая инициализация

При первом запуске или после потери БД, система **автоматически** создаст все таблицы и импортирует вопросы.

### Просто запустите:

```bash
npm start
```

Система автоматически:
1. ✅ Создаст все 8 таблиц
2. ✅ Импортирует 1130 вопросов × 3 языка = 3390 записей
3. ✅ Создаст тестовых пользователей (admin/admin, user/user)
4. ✅ Запустит сервер

## 🔄 Ручная инициализация (опционально)

Если нужно пересоздать БД вручную:

```bash
npm run init-db
```

## 🚨 Проблема
База данных PostgreSQL в Docker контейнере может быть потеряна при:
- Удалении контейнера
- Очистке volumes Docker
- Сбоях системы
- Перезапуске Docker без volume mapping

## ✅ Решение: Автоматическая инициализация

### 1️⃣ Проверка состояния базы данных

```bash
# Проверить запущен ли Docker контейнер
docker ps

# Посмотреть список таблиц в базе
docker exec -it godrive-postgres psql -U postgres -d questions -c "\dt"
```

**Должны быть таблицы:**
- `users` - пользователи
- `user_progress` - прогресс по билетам
- `user_statistics` - общая статистика (время, правильные ответы)
- `user_answers` - история ответов
- `user_mistake_stats` - статистика ошибок
- `questions_uz` - вопросы на узбекском
- `questions_ru` - вопросы на русском
- `questions_uzk` - вопросы на кириллице (узбекский)

### 2️⃣ Запуск сервера (автоматическая инициализация)

Просто запустите сервер:

```bash
npm start
```

**Система автоматически:**
- Проверит наличие таблиц
- Если таблиц нет - создаст их
- Если вопросов нет - импортирует из `data/questions.json`
- Создаст тестовых пользователей
- Запустит сервер

Никаких ручных действий не требуется!

### 3️⃣ Ручная инициализация (опционально)

Если хотите пересоздать БД вручную:

### 3️⃣ Ручная инициализация (опционально)

Если хотите пересоздать БД вручную:

```bash
npm run init-db
```

Скрипт выполнит:
- ✅ Создание всех 8 таблиц
- ✅ Создание индексов
- ✅ Импорт 3390 вопросов (1130 × 3 языка)
- ✅ Создание тестовых пользователей
- 🖼️ Автоматическая подстановка defaultpic.webp для вопросов без картинки

## 📋 Структура базы данных

### Таблица: `users`
```sql
- id (SERIAL PRIMARY KEY)
- username (VARCHAR, UNIQUE)
- password (VARCHAR)
- name (VARCHAR)
- role (VARCHAR: 'user' или 'admin')
- favorites (JSONB) - массив ID избранных вопросов
- created_at, updated_at (TIMESTAMP)
```

### Таблица: `user_statistics`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (INTEGER, FK -> users)
- time_spent_seconds (INTEGER) - общее время на сайте
- total_questions_answered (INTEGER) - всего ответов
- correct_answers (INTEGER) - правильных ответов
- created_at, updated_at (TIMESTAMP)
```

### Таблица: `user_answers`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (INTEGER, FK -> users)
- question_id (INTEGER) - ID вопроса
- user_answer (INTEGER) - выбранный вариант
- is_correct (BOOLEAN) - правильность ответа
- time_spent (INTEGER) - время на вопрос (секунды)
- created_at (TIMESTAMP)
```

### Таблица: `user_mistake_stats`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (INTEGER, FK -> users)
- question_id (INTEGER)
- mistake_count (INTEGER) - количество ошибок на этом вопросе
- last_mistake_at (TIMESTAMP)
- created_at (TIMESTAMP)
- UNIQUE(user_id, question_id)
```

### Таблица: `user_progress`
```sql
- id (SERIAL PRIMARY KEY)
- user_id (INTEGER, FK -> users)
- ticket_id (INTEGER) - номер билета
- completed (BOOLEAN) - завершён ли билет
- score (INTEGER) - количество правильных ответов
- total_questions (INTEGER) - всего вопросов в билете
- answers (JSONB) - массив ответов
- started_at, completed_at (TIMESTAMP)
- UNIQUE(user_id, ticket_id)
```

### Таблицы: `questions_uz`, `questions_ru`, `questions_uzk`
```sql
- question_id (INTEGER PRIMARY KEY)
- question_text (TEXT) - текст вопроса
- options (JSONB) - массив вариантов ответа
- correct_answer (INTEGER) - номер правильного ответа
- explanation (TEXT) - объяснение
- image_url (TEXT) - URL картинки из Cloudflare R2
  * Для вопросов с картинками: https://pub-eb6a742d1f3d48568bcc6d3c14150eaf.r2.dev/q0001.webp
  * Для вопросов без картинок: https://pub-eb6a742d1f3d48568bcc6d3c14150eaf.r2.dev/defaultpic.webp
- created_at (TIMESTAMP)
```

## 🔍 Полезные команды PostgreSQL

```bash
# Подключиться к базе данных
docker exec -it godrive-postgres psql -U postgres -d questions

# Внутри psql:
\dt                           # Список таблиц
\d table_name                 # Структура таблицы
SELECT COUNT(*) FROM users;   # Количество записей
\q                            # Выход
```

## 📊 Что работает после восстановления

✅ **Таймер** - подсчет времени на сайте (user_statistics.time_spent_seconds)  
✅ **Избранное** - добавление/удаление вопросов (users.favorites)  
✅ **Счётчик правильных/неправильных** - процент успеха (user_statistics)  
✅ **История ответов** - все попытки сохраняются (user_answers)  
✅ **Статистика ошибок** - сложные вопросы (user_mistake_stats)  
✅ **Прогресс по билетам** - завершённые билеты (user_progress)  

## ⚠️ Важно!

1. **SQLite полностью удалён** - проект использует только PostgreSQL
2. **Источник правды** - файл `data/questions.json` (хранится в git)
3. **Docker Volume** - данные хранятся в volume `godrive-postgres-data`
4. **Backup** - делай резервные копии через `pg_dump`:

```bash
docker exec godrive-postgres pg_dump -U postgres questions > backup.sql
```

5. **Restore из backup**:

```bash
docker exec -i godrive-postgres psql -U postgres -d questions < backup.sql
```

## 🔄 Автоматизация

**Встроенная автоматическая инициализация:**

```bash
npm start          # Запуск с автопроверкой и инициализацией БД
npm run init-db    # Ручная инициализация/пересоздание БД
```

**Принцип работы:**
1. При старте сервер проверяет наличие таблицы `users`
2. Если таблицы нет → автоматически запускается init.js
3. init.js создаёт все таблицы и импортирует вопросы
4. Сервер запускается

**Никаких костылей!** Запускайте в любой среде - всё настроится автоматически.

## 📝 История изменений

- **12.12.2025** - Полное восстановление после потери БД
- Созданы скрипты: `restore-tables.js`, `import-questions.js`
- Удалён SQLite, оставлен только PostgreSQL
- Добавлена таблица `user_mistake_stats`
- Обновлён `schema.sql` со всеми таблицами
