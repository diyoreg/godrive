# 🚗 GoDrive - Backend Setup Instructions

## 📋 Системные требования

- **Node.js**: версия 14.0.0 или выше
- **npm**: версия 6.0.0 или выше  
- **SQLite**: встроен в проект (дополнительная установка не требуется)

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Инициализация базы данных
```bash
npm run init-db
```

### 3. Запуск сервера
```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Продакшн режим
npm start
```

## 📊 Структура проекта

```
godrive/
├── 📁 database/           # База данных SQLite
│   ├── init.js           # Инициализация БД
│   ├── connection.js     # Подключение к БД
│   ├── schema.sql        # SQL схема
│   └── godrive.db        # SQLite файл (создается автоматически)
├── 📁 models/            # Модели данных
│   ├── User.js           # Модель пользователя
│   └── Progress.js       # Модель прогресса
├── 📁 routes/            # API маршруты
│   ├── auth.js           # Аутентификация
│   ├── users.js          # Управление пользователями
│   └── progress.js       # Прогресс по билетам
├── 📁 middleware/        # Middleware
│   └── auth.js           # JWT аутентификация
├── server.js             # Express сервер
├── package.json          # Зависимости Node.js
└── [frontend files]      # HTML/CSS/JS файлы
```

## 🔗 API Endpoints

### 🔐 Аутентификация
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/profile` - Профиль пользователя  
- `PUT /api/auth/profile` - Обновление профиля
- `POST /api/auth/clear-data` - Очистка данных пользователя

### 👥 Пользователи (только админы)
- `GET /api/users` - Список пользователей
- `POST /api/users` - Создание пользователя
- `PUT /api/users/:id` - Обновление пользователя
- `DELETE /api/users/:id` - Удаление пользователя

### 📈 Прогресс
- `GET /api/progress` - Прогресс пользователя
- `POST /api/progress/ticket/:id` - Сохранение прогресса билета
- `GET /api/progress/stats` - Статистика пользователя
- `DELETE /api/progress` - Очистка всего прогресса

## 🎯 Пользователи по умолчанию

После инициализации БД создаются следующие пользователи:

| Логин | Пароль | Роль | Имя |
|-------|--------|------|-----|
| admin | M0h1zAQ-godrive | admin | Администратор |
| test | test123 | user | Тестовый пользователь |
| user1 | pass1 | user | Пользователь 1 |
| demo | demo | user | Демо пользователь |

## 🛠️ Полезные команды

```bash
# Установка зависимостей
npm install

# Инициализация БД (создание таблиц и пользователей)
npm run init-db

# Запуск в режиме разработки
npm run dev

# Запуск в продакшн режиме
npm start

# Проверка работы API
curl http://localhost:3000/api/health
```

## 🔧 Переменные окружения

Создайте файл `.env` для настройки:

```env
# Порт сервера (по умолчанию 3000)
PORT=3000

# Секрет для JWT токенов
JWT_SECRET=your_secret_key_here

# Время жизни JWT токенов (по умолчанию 24h)
JWT_EXPIRES_IN=24h

# Режим работы (development/production)
NODE_ENV=development
```

## 📱 Доступ к приложению

После запуска сервера:

- **Frontend**: http://localhost:3000
- **API документация**: http://localhost:3000/api/health
- **База данных**: `database/godrive.db` (SQLite файл)

## 🐛 Отладка

### Проверка состояния сервера:
```bash
curl http://localhost:3000/api/health
```

### Логи сервера:
Все запросы логируются в консоль с временными метками.

### Проверка БД:
```bash
# Войти в SQLite CLI
sqlite3 database/godrive.db

# Показать таблицы
.tables

# Показать пользователей
SELECT * FROM users;
```

## 📚 Технологии

- **Backend**: Node.js + Express.js
- **База данных**: SQLite3
- **Аутентификация**: JWT (JSON Web Tokens)
- **Безопасность**: Helmet, CORS, Rate Limiting  
- **Хеширование паролей**: bcryptjs

## 🚀 Развертывание

1. Скопируйте проект на сервер
2. Установите зависимости: `npm install`
3. Инициализируйте БД: `npm run init-db`
4. Установите переменные окружения
5. Запустите: `npm start`

## ⚠️ Важные замечания

- **Бэкап БД**: Регулярно копируйте файл `database/godrive.db`
- **Безопасность**: Измените JWT_SECRET в продакшене
- **Порт**: Убедитесь, что порт 3000 доступен
- **HTTPS**: В продакшене используйте HTTPS для безопасности