# 🚗 GoDrive - Экзаменационные билеты ПДД

Веб-приложение для подготовки к экзамену на водительские права в Узбекистане.

## ✨ Возможности

- 📚 **1130 вопросов** в 113 билетах (по 10 вопросов)
- 🌍 **3 языка**: Русский, O'zbekcha, Ўзбекча
- ⭐ **Избранные вопросы** для повторения
- 📊 **Статистика прогресса** по билетам
- 💡 **Объяснения** к ответам
- 🎨 **Современный UI** с анимациями

## 🚀 Деплой на Railway (рекомендуется)

### 1. Создать проект
```bash
# Зайти на railway.app
# New Project → Deploy from GitHub repo
# Выбрать: diyoreg/godrive
```

### 2. Добавить PostgreSQL
```bash
# В Railway dashboard:
# Add Service → PostgreSQL
```

### 3. Установить переменные окружения
Скопируй credentials из PostgreSQL service:
```env
POSTGRES_HOST=xxx.railway.app
POSTGRES_PORT=5432
POSTGRES_DB=railway
POSTGRES_USER=postgres
POSTGRES_PASSWORD=xxx_auto_generated_xxx

JWT_SECRET=generate-random-32-chars-string
NODE_ENV=production
PORT=3000
```

### 4. Готово! 🎉
Railway автоматически задеплоит за 2-3 минуты.

## 💻 Локальная разработка

```bash
# 1. Клонировать
git clone https://github.com/diyoreg/godrive.git
cd godrive

# 2. Установить зависимости
npm install

# 3. Создать .env файл
cp .env.example .env
# Отредактировать .env с локальными настройками PostgreSQL

# 4. Запустить
npm start

# 5. Открыть
http://localhost:3000
```

## 📁 Структура проекта

```
godrive/
├── server.js              # Express сервер
├── package.json           # Зависимости
├── Procfile              # Railway конфигурация
│
├── database/             # База данных
│   ├── connection.js     # Подключение к PostgreSQL
│   ├── init.js          # Инициализация БД
│   └── schema.sql       # Схема таблиц
│
├── routes/              # API маршруты
│   ├── auth.js         # Авторизация
│   ├── progress.js     # Прогресс
│   ├── favorites.js    # Избранное
│   └── stats.js        # Статистика
│
├── models/             # Модели данных
│   ├── User.js
│   ├── Progress.js
│   └── UserStats.js
│
├── middleware/         # Middleware
│   └── auth.js        # JWT аутентификация
│
├── js/                # Frontend JavaScript
│   ├── api-client.js  # API клиент
│   ├── auth.js        # Авторизация
│   ├── dashboard.js   # Дашборд
│   └── language.js    # Локализация
│
├── data/              # Данные
│   └── questions/     # 1130 вопросов в JSON
│
└── *.html            # Frontend страницы
    ├── index.html    # Главная
    ├── login.html    # Вход
    ├── dashboard.html # Дашборд с билетами
    ├── ticket.html   # Страница билета
    └── favorites.html # Избранные вопросы
```

## 🔑 API Endpoints

### Авторизация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация (только админ)

### Прогресс
- `GET /api/progress` - Весь прогресс пользователя
- `POST /api/progress/ticket/:id` - Сохранить прогресс билета

### Избранное
- `GET /api/favorites` - Список избранных
- `POST /api/favorites` - Добавить в избранное
- `DELETE /api/favorites` - Удалить из избранного

### Статистика
- `GET /api/stats` - Статистика пользователя

## 🔧 Технологии

- **Backend**: Node.js, Express
- **Database**: PostgreSQL
- **Auth**: JWT (jsonwebtoken)
- **Security**: Helmet, CORS, Rate Limiting
- **Frontend**: Vanilla JavaScript, CSS3

## 📝 Первый запуск

При первом запуске автоматически:
1. ✅ Создаются таблицы в БД
2. ✅ Создаётся администратор: `admin` / `admin123`

## 🐛 Troubleshooting

### Ошибка подключения к БД
Проверь переменные `POSTGRES_*` в Railway dashboard.

### 404 на вопросы
Убедись что папка `data/questions/` загружена в git.

### JWT ошибки
Проверь что `JWT_SECRET` установлен и длиннее 32 символов.

## 📜 Лицензия

MIT

## 👤 Автор

diyoreg

---

**Нужна помощь?** Проверь файлы:
- `RAILWAY_CHECKLIST.md` - детальная инструкция
- `RAILWAY.md` - полная документация
