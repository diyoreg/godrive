# GoDrive - Railway Deployment

## Переменные окружения для Railway

Создайте следующие переменные в Railway dashboard:

### PostgreSQL (обязательно)
```
POSTGRES_HOST=your-postgres-host
POSTGRES_PORT=5432
POSTGRES_DB=questions
POSTGRES_USER=your-postgres-user
POSTGRES_PASSWORD=your-postgres-password
```

### JWT Authentication (обязательно)
```
JWT_SECRET=your-secure-random-string-here
```

### Optional (есть значения по умолчанию)
```
NODE_ENV=production
PORT=3000
```

## Миграции базы данных

После первого деплоя выполните миграции:

```bash
# 1. Добавить колонку favorites
railway run node database/add-favorites-column.js

# 2. Добавить таблицу user_statistics
railway run node database/add-user-statistics.js
```

## Автоматическая инициализация

При первом запуске сервер автоматически:
- ✅ Создаст таблицу users (если не существует)
- ✅ Создаст таблицу user_progress (если не существует)
- ✅ Создаст администратора (если пользователей нет)

## Проверка здоровья

После деплоя проверьте:
- `https://your-app.railway.app/` - должна открыться главная страница
- `https://your-app.railway.app/api/health` - должен вернуть `{"status": "ok"}`

## Структура API

- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/progress` - прогресс пользователя
- `POST /api/progress/ticket/:id` - сохранить прогресс билета
- `GET /api/favorites` - получить избранные
- `POST /api/favorites` - добавить в избранное
- `DELETE /api/favorites` - удалить из избранного
- `GET /api/stats` - статистика пользователя
- `POST /api/stats/update` - обновить статистику

## Новые функции

### Избранные вопросы
- ⭐ Добавление/удаление вопросов в избранное
- 📂 Страница со всеми избранными вопросами
- 🔢 Счетчик избранных в дашборде

### Статистика пользователя
- ⏱️ Время, проведённое в приложении
- ✅ Количество отвеченных вопросов
- 📊 Процент правильных ответов

### UX улучшения
- 🎨 Горизонтальный скролл для множества вопросов
- 🌍 Полная локализация (ru/uz/uzk)
- 💡 Сворачиваемые объяснения
- 🎯 Автопрокрутка к активной кнопке
